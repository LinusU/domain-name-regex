{
  "name": "domain-name-regex",
  "version": "2.0.0",
  "licence": "MIT",
  "repository": "LinusU/domain-name-regex",
  "scripts": {
    "test": "standard && mocha"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "standard": "^12.0.1"
  }
}
